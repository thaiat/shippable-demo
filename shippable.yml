language: node_js

node_js:
  - 0.10.25
install:
  - npm install -g karma karma-cli bower
  - npm install
  - bower install
# Make folders for the reports, and create the SQL db
before_script:


script:
  - npm test
  - mv shippable/codecoverage/*/coverage.xml shippable/codecoverage
after_script:

after_success :
  - git push  git@heroku.com:avi-hot-static.git master
  - echo $BRANCH
  - echo $BUILD_NUMBER
  - echo $BUILD_URL 
  - echo $COMMIT 
  - echo $DEBIAN_FRONTEND 
  - echo $JOB_ID 
  - echo $PULL_REQUEST 
  - gulp
