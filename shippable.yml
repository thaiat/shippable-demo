language: node_js

node_js:
  - 0.10.25
install:
  - npm install -g karma karma-cli bower
  - npm install
  - bower install
# Make folders for the reports, and create the SQL db
before_script:


script:
  - npm test

after_script:
  - mv /shippable/codecoverage/*/coverage.xml /shippable/codecoverage/coverage.xml